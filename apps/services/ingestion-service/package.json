{
  "name": "@wattweiser/ingestion-service",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 3008",
    "start": "uvicorn main:app --host 0.0.0.0 --port 3008",
    "lint": "ruff check . || echo 'ruff not installed, skipping'",
    "format": "ruff format . || echo 'ruff not installed, skipping'",
    "type-check": "mypy . || echo 'mypy not installed, skipping type-check for Python service'"
  }
}



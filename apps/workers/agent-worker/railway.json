{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/workers/agent-worker && npm install && npm run build"
  },
  "deploy": {
    "startCommand": "cd apps/workers/agent-worker && npm run start:prod",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "healthcheckPath": "/health",
    "healthcheckTimeout": 100,
    "healthcheckInterval": 10,
    "scaling": {
      "minReplicas": 1,
      "maxReplicas": 2,
      "targetCPU": 50,
      "targetMemory": 60
    }
  }
}

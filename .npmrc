# pnpm Configuration für Windows
# Hoisted node-linker löst Probleme mit langen Pfaden und fehlenden .EXE-Dateien
node-linker=hoisted

# Auto-install peers
auto-install-peers=true

# Strict peer dependencies (wird in CI/CD geprüft)
strict-peer-dependencies=false

# Shamefully hoist (Fallback, falls hoisted nicht ausreicht)
# shamefully-hoist=true





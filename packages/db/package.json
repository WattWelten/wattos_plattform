{
  "name": "@wattweiser/db",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "generate": "prisma generate",
    "build": "tsc",
    "build:full": "pnpm generate && tsc",
    "dev": "tsc --watch",
    "type-check": "tsc --noEmit",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "studio": "prisma studio",
    "format": "prisma format",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.12",
    "@nestjs/config": "^4.0.2",
    "@prisma/adapter-pg": "^7.3.0",
    "@prisma/client": "^7.3.0",
    "pg": "^8.17.2"
  },
  "devDependencies": {
    "@types/pg": "^8.11.10",
    "prisma": "^7.2.0",
    "typescript": "^5.9.3"
  }
}
